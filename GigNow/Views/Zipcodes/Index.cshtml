@model IEnumerable<GigNow.Models.Zipcode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.address.StreetAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ZipCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.address.StreetAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ZipCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZipcodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZipcodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZipcodeId })
        </td>
    </tr>
}

</table>
