@model IEnumerable<GigNow.Models.ListenerViewModelVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ListenerViewModel }) |
            @Html.ActionLink("Details", "Details", new { id=item.ListenerViewModel }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ListenerViewModel })
        </td>
    </tr>
}

</table>
