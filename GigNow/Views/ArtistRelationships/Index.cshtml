@model IEnumerable<GigNow.Models.ArtistRelationship>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artist.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Listener.UserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artist.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Listener.UserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtistRelationshipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtistRelationshipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtistRelationshipId })
        </td>
    </tr>
}

</table>
