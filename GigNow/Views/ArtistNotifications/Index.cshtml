@model IEnumerable<GigNow.Models.ArtistNotification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtistNotificationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtistNotificationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtistNotificationId })
        </td>
    </tr>
}

</table>
